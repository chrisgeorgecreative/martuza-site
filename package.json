{
  "name": "martuza-site",
  "version": "2.0.0",
  "description": "Personal site for Chris Martuza (Stuzie) — service designer & design leader.",
  "private": true,
  "scripts": {
    "dev": "browser-sync start --server . --files \"*.html, partials/*.html, css/*.css, js/*.js\" --no-notify --startPath index.html",
    "build": "node build.js"
  },
  "devDependencies": {
    "browser-sync": "^3.0.2",
    "clean-css": "^5.3.3",
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.31.0"
  }
}
